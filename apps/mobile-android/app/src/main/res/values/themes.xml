<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws its own UI; the Activity theme only controls window chrome. -->
    <style name="Theme.MaimaiHome" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
