<?xml version="1.0" encoding="utf-8"?>
<!--
  Robolectric's debug unit-test manifest is assembled separately from the debug
  application manifest. Mirror the debug test host here so createComposeRule()
  can resolve ComponentActivity for its MAIN/LAUNCHER launch intent.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name="androidx.activity.ComponentActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
