<?xml version="1.0" encoding="utf-8"?>
<!--
  Wave 5: Robolectric manifest for release unit tests.
  Registers ComponentActivity so createComposeRule() can resolve the activity
  in the release variant (the debug variant gets this from ui-test-manifest).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name="androidx.activity.ComponentActivity"
            android:exported="false" />
    </application>
</manifest>
