<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug unit-test host for Robolectric Compose tests. createComposeRule() starts
  ComponentActivity with a MAIN/LAUNCHER intent, so declare that intent here.
  This debug-only overlay is not packaged into release builds.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name="androidx.activity.ComponentActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
