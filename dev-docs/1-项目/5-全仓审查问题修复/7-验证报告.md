---
title: 全仓审查问题修复文档迁移计划与验证记录
document_type: verification-report
status: draft
initiative: 全仓审查问题修复
audience:
  - 文档维护者
  - 技术负责人
  - 发布负责人
owners:
  - Maimai Dev
created: 2026-08-02
updated: 2026-08-02
source_of_truth:
  - 4-技术设计.md
related:
  - 1-概览.md
  - 3-验收标准.md
  - 5-实现计划.md
  - 8-调研/4-审查问题处置台账.md
---

# 全仓审查问题修复文档迁移计划与验证记录

本文记录文档治理，不是产品实现验证报告。当前行为以源码/配置/实际命令为准；未来协议只以[技术设计](4-技术设计.md)为准。

## 已交付范围

- 将本项目的产品需求、验收、技术设计、计划、测试、UX、台账和迁移记录统一纳入 docctl 认可的项目文件/`research` 命名与类型。
- 新增 `3-验收标准.md`，消除需求中派生 AC 表；`4-技术设计.md` 保持唯一技术入口，无 `2-总体技术设计.md` 双入口。
- 全局索引仅通过 docctl 重建；本文档目录内不存在空入口。

## 实际变化

| DOC-ID | 项目 | 权威性与动作 | 状态/关闭条件 |
|---|---|---|---|
| DOC-01 | 本项目工件 | 固定 `1-概览`、`2-产品需求`、`3-验收标准`、`4-技术设计`、`5-实现计划`、`6-发布计划`、`7-验证报告`；UX/测试/台账留在 `8-调研` 并用 `research`。 | 本次完成；docctl 通过。 |
| DOC-02 | REQ/AC/技术入口 | REQ 只在 `2-产品需求`，AC 只在 `3-验收标准`，技术决策、API、错误和迁移只在 `4-技术设计`。 | 本次完成；后续不得派生 REQ/AC。 |
| DOC-03 | 现有架构/README/reference 漂移 | Windows Agent、PC Web、Android 架构和 README 中的当前事实必须待实现后按源码、契约测试和候选日志更新；不得提前写入 revision、event v2、cursor v2 或同源导航为 current。 | 待 P0/P1 的源码与证据。 |
| DOC-04 | 命令、ABI、测试数和性能 | 命令以 package/Gradle/csproj/脚本为准；测试数、APK 大小、性能和设备通过状态仅能来自本次原始日志。 | Agent 与 Android JVM 基线阻断已关闭；候选的性能、APK 与设备证据仍待执行。 |
| DOC-05 | 历史工件 | Round-2/3/4 和 `99-归档` 仅作历史证据，不是 current 技术权威；HUI 只复验台账结论。 | 持续；失败项另立项。 |

## 与计划的偏差

最初并行写入造成 AC 重复、实现计划引用不存在的 REQ-13/NFR/AC-12/13，以及 UX 使用派生 `REQ-WEB-*`/`REQ-AND-*`。本次将这些关系收敛到 REQ-01～12、AC-01～11、ADR-01～10 和 TEST-ID。产品代码、测试代码、基础设施和发布配置未由本任务修改。

## 验证证据

| 命令 | 实际结果 | 可得结论 |
|---|---|---|
| `node "C:\Users\abbey\.agents\skills\dev-doc-writer\scripts\docctl.mjs" rebuild-index --root dev-docs` | 必须在最终文档修改后执行，成功后重写 `0-索引.md`。 | 未执行前不可声称索引已按最终 front matter 重建。 |
| `node "C:\Users\abbey\.agents\skills\dev-doc-writer\scripts\docctl.mjs" validate --root dev-docs` | 本次修改前已返回 `ok: true`、0 errors、0 warnings；最终修改后必须再次执行并以最终输出为准。 | 不可把修改前结果冒充最终结果。 |
| `pnpm --dir apps/pc-web test` | 9 文件、55 测试通过。 | 仅现有 PC Web 基线。 |
| `dotnet test services/windows-agent/tests/MaimaiHomeAgent.Tests/MaimaiHomeAgent.Tests.csproj --configuration Release --no-restore` | `PathGuardTests.ResolveSafe_RejectsJunctionPointingOutsideRoot` 的链接目标逃逸断言已对齐为 `PathSafetyError.SymlinkEscape`；完整 Agent Release 测试 231/231 通过。 | Agent 基线阻断已关闭；不证明 P0 已实现。 |
| `apps/mobile-android` 中 `./gradlew.bat :app:testDebugUnitTest :app:testReleaseUnitTest` | `AudioViewModelTest.connectionStateChange_updatesConnectionText()` 的连接文案断言已改为“已连接”；Android Release 完整单测通过，完整 Debug 与 Release 单测均 `BUILD SUCCESSFUL`。 | Android 文案基线阻断已关闭；不证明 P0 已实现或可发布。 |
| Android Compose Robolectric Screen 回归 | `src/debug/AndroidManifest.xml` 与 `src/testDebug/AndroidManifest.xml` 显式提供 `ComponentActivity` 的 `MAIN/LAUNCHER` launcher host；Audio、Connection、Files、Power 四类 Screen 共 39 项通过。 | Android Robolectric host 基线阻断已关闭；仅覆盖 JVM/Robolectric。 |

## 文档同步

实施后按下表更新，且每项必须链接源码、契约测试或候选日志：

| 触发 | 更新目标 | 必需证据 |
|---|---|---|
| P0-01 | 根 README、PC Web 架构 | 页面 origin、相对 API/WS、浏览器双 origin 证据。 |
| P0-02/03 | Android 架构、README | 五 Tab、Admin→Power、draft 行为和 arm64 设备证据。 |
| P0-04/05 | Agent 架构、事件参考 | revision/ETag/If-Match、唯一仓储、event v2 的契约测试。 |
| P0-06 | Agent/PC/Android 文件说明 | cursor v2/v1 capability、严格全序、错误和目录变更测试。 |
| P0-07 | PC/Agent 操作说明 | Launcher runtime、无害脚本 E2E 和错误契约。 |
| 发布候选 | README、发布指南、概览、索引 | SHA、hash、完整门禁、回滚和未验证项。 |

## 部署与迁移

先 Agent、后 PC Web、再 Android；具体兼容窗口和回滚责任待发布负责人批准。既有基线测试阻断已关闭，但 P0 实现尚未开始；工作树隔离、候选提交记录和 P0 证据均属于下一阶段。任何 pending journal、P0 缺证据或设备/桌面环境阻塞都禁止发布。本文未执行部署。

## 残余风险

当前工作树含并行改动，尚未完成隔离或记录候选提交；P0 实现尚未开始。真实 arm64、交互式 Windows、双 origin 浏览器和性能环境未执行；10k/50k 等大目录验证未执行，cursor 无索引时仍可能扫描目录；设置与 AutoStart 不提供硬事务。这些风险禁止将项目状态提升为“P0 已完成”或“可发布”。
